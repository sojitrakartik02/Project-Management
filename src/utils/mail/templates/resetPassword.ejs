<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Password</title>
    <style>
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
            line-height: 1.6;
            color: #333333;
        }

        .container {
            max-width: 600px;
            margin: 40px auto;
            background: #ffffff;
            border-radius: 10px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            animation: fadeIn 0.5s ease-in;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .header {
            background: linear-gradient(90deg, #ff6200, #e65500);
            /* Gradient orange */
            color: #ffffff;
            padding: 25px;
            text-align: center;
            position: relative;
            border-bottom: 5px solid #d94f00;
        }

        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://via.placeholder.com/600x100?text=Your+Logo') no-repeat center;
            background-size: contain;
            opacity: 0.1;
        }

        .content {
            padding: 40px;
            text-align: center;
        }

        .content p {
            margin: 0 0 20px;
            font-size: 16px;
            color: #555555;
        }

        .content strong {
            color: #ff6200;
            font-weight: 600;
        }

        .verify-link {
            margin: 30px 0;
            display: inline-block;
        }

        .verify-button {
            display: inline-block;
            background: linear-gradient(90deg, #ff6200, #e65500);
            /* Gradient button */
            color: #ffffff;
            padding: 15px 35px;
            text-decoration: none;
            font-size: 18px;
            font-weight: 600;
            border-radius: 50px;
            /* Rounded button */
            border: 2px solid #ffffff;
            box-shadow: 0 4px 15px rgba(255, 98, 0, 0.3);
            transition: all 0.3s ease;
        }

        .verify-button:hover {
            background: linear-gradient(90deg, #e65500, #ff6200);
            box-shadow: 0 6px 20px rgba(255, 98, 0, 0.4);
            transform: translateY(-2px);
        }

        .note {
            font-size: 14px;
            color: #777777;
            margin-top: 15px;
            font-style: italic;
        }

        .footer {
            text-align: center;
            font-size: 12px;
            color: #999999;
            padding: 20px;
            background: #fafafa;
            border-top: 1px solid #eeeeee;
        }

        .footer a {
            color: #ff6200;
            text-decoration: none;
            margin: 0 10px;
            transition: color 0.3s ease;
        }

        .footer a:hover {
            color: #e65500;
        }

        .footer .social-icons {
            margin-top: 10px;
        }

        .footer .social-icons a {
            display: inline-block;
            margin: 0 5px;
            font-size: 16px;
            color: #ff6200;
        }

        /* Ensure responsiveness */
        @media only screen and (max-width: 600px) {
            .container {
                margin: 20px;
                padding: 0;
            }

            .content {
                padding: 25px;
            }

            .header h1 {
                font-size: 22px;
            }

            .verify-button {
                padding: 12px 25px;
                font-size: 16px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>Create New Password</h1>
        </div>
        <div class="content">
            <p>Hello <strong>
                    <%= name %>
                </strong>,</p>
            <p>We’ve received a request to reset your password. Take action now to secure your <strong>
                    <%= appName %>
                </strong> account by clicking the button below:</p>
            <div class="verify-link">
                <a href="<%= mailLink %>" class="verify-button">Create New Password</a>
            </div>
            <p class="note">This link is valid for the next 15 minutes. If you didn’t initiate this request, please
                ignore this email.</p>
            <p>Best regards,</p>
            <p>The <strong>
                    <%= appName %>
                </strong> Team</p>
        </div>
        <div class="footer">
            <p>© <%= year %>
                    <%= appName %>. All rights reserved.</p>
            <p><a href="mailto:support@<%= appName %>.com">Contact Support</a> | <a
                    href="https://<%= appName %>.com/privacy">Privacy Policy</a></p>
            <div class="social-icons">
                <a href="https://facebook.com/<%= appName %>" aria-label="Facebook">🌐</a>
                <a href="https://twitter.com/<%= appName %>" aria-label="Twitter">🐦</a>
                <a href="https://linkedin.com/company/<%= appName %>" aria-label="LinkedIn">💼</a>
            </div>
        </div>
    </div>
</body>

</html>